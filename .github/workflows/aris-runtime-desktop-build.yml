name: aris-runtime-desktop-build

on:
  workflow_dispatch:
  push:
    paths:
      - "pyproject.toml"
      - "LOGBOOK.md"
      - "evolving_ai/aris_runtime/**"
      - "tests/test_aris_runtime.py"
      - "tests/test_aris_runtime_desktop.py"

jobs:
  build:
    name: build-${{ matrix.platform.id }}
    runs-on: ${{ matrix.platform.os }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - id: windows
            os: windows-latest
            artifact_name: aris-runtime-desktop-windows
            bundle_path: artifacts/dist/ARIS V2
          - id: macos
            os: macos-latest
            artifact_name: aris-runtime-desktop-macos
            bundle_path: artifacts/dist/ARIS V2.app
          - id: linux
            os: ubuntu-latest
            artifact_name: aris-runtime-desktop-linux
            bundle_path: artifacts/dist/ARIS V2

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install desktop build dependencies
        run: python -m pip install -e .[desktop-build]

      - name: Run targeted runtime tests
        run: >
          python -m unittest
          tests.test_aris_runtime
          tests.test_aris_runtime_desktop_runtime
          tests.test_aris_runtime_shipping_lane

      - name: Run desktop smokecheck
        run: python -m evolving_ai.aris_runtime.desktop --headless-smokecheck --no-workers

      - name: Build native bundle
        run: >
          python -m evolving_ai.aris_runtime.desktop_build
          --build-current
          --distpath artifacts/dist
          --workpath artifacts/work
          --specpath artifacts/spec

      - name: Upload native bundle
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform.artifact_name }}
          path: ${{ matrix.platform.bundle_path }}
          if-no-files-found: error
